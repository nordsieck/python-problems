import unittest
from typing import *

class Solution:


class TestSolution(unittest.TestCase):
    def testFn(self):
        s = Solution()
        data = [
        ]

        for a, b in data:
            self.assertEqual(s.fn(a), b)
